#include <stdio.h>
#include <stdlib.h>

int main(){
	// system("color f3"); // Não é multiplataforma.
	char letra;
	for (letra = 17; letra <= 126; letra++) printf("%c", letra);
	return(0);
}
